#include "main.h"

/**
 * _isdigit - checks if a value is a digit
 * @x: number to be checked
 * Return: 1 for a digit character and 0 for else
 */
int _isdigit(int x)
{
	if (x >= 48 && x <= 57)
	{
		return (1);
	}
}
